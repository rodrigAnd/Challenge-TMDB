<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Custom styles for Material Components -->
    
    <!-- TextInputLayout Outlined Style -->
    <style name="Widget.MyChallenge.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">?attr/colorPrimary</item>
        <item name="hintTextColor">?attr/colorPrimary</item>
        <item name="boxStrokeWidth">1.5dp</item>
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
        <item name="boxBackgroundMode">outline</item>
    </style>
    
    <!-- Card Style -->
    <style name="Widget.MyChallenge.Card" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="strokeWidth">0dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>
    
    <!-- Button Style -->
    <style name="Widget.MyChallenge.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
    
    <!-- BottomSheet Style -->
    <style name="Widget.MyChallenge.BottomSheet" parent="Widget.MaterialComponents.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.MyChallenge.BottomSheet</item>
        <item name="android:background">@drawable/bottom_sheet_background</item>
    </style>
    
    <!-- Shape Appearance for BottomSheet -->
    <style name="ShapeAppearanceOverlay.MyChallenge.BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeTopRight">24dp</item>
    </style>
    
    <!-- AppBar Overlay Theme -->
    <style name="Theme.MyChallenge.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    
    <!-- Popup Overlay Theme -->
    <style name="Theme.MyChallenge.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    
</resources>
