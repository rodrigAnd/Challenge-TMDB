<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:name=".MyChallengeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyChallenge"
        tools:targetApi="31">
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyChallenge.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyChallenge" />
        
        <activity
            android:name=".presentation.movieDetail.MovieDetailActivity"
            android:exported="false"
            android:label="@string/movie_details"
            android:theme="@style/Theme.MyChallenge" />
        
        <activity
            android:name=".presentation.error.ErrorConnectionActivity"
            android:exported="false"
            android:label="@string/no_internet_connection"
            android:theme="@style/Theme.MyChallenge" />
        
        <activity
            android:name=".presentation.error.ErrorActivity"
            android:exported="false"
            android:label="@string/error_occurred"
            android:theme="@style/Theme.MyChallenge" />
    </application>

</manifest>